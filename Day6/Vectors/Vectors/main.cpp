//
//  main.cpp
//  Vectors
//
//  Created by Jia Gao on 8/30/24.
//

#include <iostream>
#include <vector>



/ Function stringToVec
///create vector store string
std::vector <char> chars;
std::vector <std::string> strings;
///return char into chars--add in chars go through the string
for(int i = 0; i< strings.size(); i++){
        chars.push_back(strings[i]);
return chars;
        
    }
/ Function reverse
    ///create a empty vector
std::vector <int> ints;
    std::vector <int> reverseInts;
    ///

//create a empty vector

//read data into numbers: for-- add in sum when a new num come in

int main(int argc, const char * argv[]) {
    std::vector <int> numbers;
    int sum = 0;
    for(int i = 0; i < numbers.size(); i++ ){
        sum += numbers[i];
    }
    std::cout << "Sum= " <<sum <<std::endl;
    return 0;
    

    
    
    
    
    
    
}
